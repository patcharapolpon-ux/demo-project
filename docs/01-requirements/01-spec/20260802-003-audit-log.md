# ระบบบันทึก Log (Audit Trail)

**วันที่:** 2026-08-02
**สถานะ:** ร่าง (Draft)

## ที่มา / บริบท

ต่อยอดจากระบบสั่งกาแฟจากโต๊ะด้วยการสแกน QR Code (ดู [[20260802-001-table-qr-ordering|ระบบสั่งกาแฟจากโต๊ะด้วยการสแกน QR Code]]) และหน้า Dashboard ดูยอดขาย (ดู [[20260802-002-sales-dashboard|หน้า Dashboard ดูยอดขาย]]) ที่มีอยู่แล้ว ร้านต้องการเก็บบันทึกการทำงาน (log) ของพนักงานในระบบ เพื่อให้สามารถตรวจสอบย้อนหลังได้กรณีเกิดปัญหาหรือข้อพิพาท (เช่น ออเดอร์ผิดพลาด, เมนูถูกปิดโดยไม่ทราบสาเหตุ) และเพื่อความปลอดภัยของข้อมูลในภาพรวม

## ความต้องการ (Requirement)

ระบบต้องบันทึก log เหตุการณ์สำคัญที่เกิดขึ้นในระบบ โดยแต่ละ log record ต้องระบุเวลาที่เกิดเหตุการณ์ (timestamp), ผู้ทำรายการ (user/role), ประเภทเหตุการณ์ และรายละเอียดที่เกี่ยวข้องกับเหตุการณ์นั้น log ที่เก็บไว้จะถูกลบออกจากระบบโดยอัตโนมัติเมื่อมีอายุเกิน 90 วัน และเฉพาะบทบาทผู้จัดการร้าน/แอดมินเท่านั้นที่มีสิทธิ์เข้าถึงหน้าดู log ย้อนหลัง บทบาทอื่น (พนักงานเสิร์ฟ, บาริสต้า) ไม่มีสิทธิ์เข้าถึง

## ขอบเขต (Scope)

### สิ่งที่ต้องทำ (In scope)
- บันทึก log เหตุการณ์สร้างออเดอร์ใหม่
- บันทึก log การเปลี่ยนสถานะออเดอร์ทุกครั้ง (รับออเดอร์ -> กำลังทำ -> เสร็จ -> เสิร์ฟแล้ว) พร้อม timestamp และผู้ทำรายการ (บาริสต้า/พนักงานเสิร์ฟคนไหน)
- บันทึก log การเปิด/ปิดเมนูรายการชั่วคราว พร้อม timestamp และผู้ทำรายการ (บาริสต้าหรือผู้จัดการร้าน)
- บันทึก log การเข้าถึงหน้า sales dashboard ว่าใคร (เจ้าของร้าน/แอดมิน) เข้าดูเมื่อไหร่
- นโยบายเก็บรักษา log: เก็บไว้ 90 วัน แล้วลบทิ้งอัตโนมัติ
- หน้าดู log ย้อนหลัง จำกัดสิทธิ์เฉพาะบทบาทผู้จัดการร้าน/แอดมิน

### สิ่งที่ไม่ทำ (Out of scope)
- การ export log เป็นไฟล์ (เช่น CSV) ในเฟสนี้
- ระบบแจ้งเตือน/alert อัตโนมัติจาก log (เช่น anomaly detection)
- log ระดับ infrastructure/server (เช่น server access log, network log) — ครอบคลุมเฉพาะ application-level business event log เท่านั้น

## กฎทางธุรกิจ / เงื่อนไข (Business Rules)
- ทุก log record ต้องมี timestamp, ผู้ทำรายการ (user/role), ประเภทเหตุการณ์ และรายละเอียดที่เกี่ยวข้อง (เช่น order id, เมนูที่ถูกปิด)
- Log ที่มีอายุเกิน 90 วันต้องถูกลบออกจากระบบโดยอัตโนมัติ (ตามหลัก data minimization) — นับจาก**เวลาที่เกิดเหตุการณ์** ของแต่ละ log record
- เฉพาะบทบาทผู้จัดการร้าน/แอดมินเท่านั้นที่เข้าถึงหน้าดู log ย้อนหลังได้ บทบาทอื่น (พนักงานเสิร์ฟ, บาริสต้า) ไม่มีสิทธิ์เข้าถึง

### การ anonymize ข้อมูลระบุตัวตนหลังพนักงานถูกปลดออกจากระบบ (Offboarding) — NFR เพิ่มเติม

> คนละเรื่องกับนโยบาย retention 90 วันข้างต้น (ซึ่งลบทั้ง record ทิ้งเมื่อ log มีอายุเกิน 90 วันนับจากเวลาที่เกิดเหตุการณ์) — ข้อนี้คือการ **anonymize** เฉพาะข้อมูลระบุตัวตนของผู้ทำรายการ โดยนับระยะเวลาจากคนละ trigger กัน (วันที่พนักงานถูกปลดออกจากระบบ ไม่ใช่วันที่เกิดเหตุการณ์) และ log record เองยังคงอยู่ในระบบต่อไปตามอายุของมัน (จนกว่าจะครบ 90 วันนับจากเวลาที่เกิดเหตุการณ์ตามเดิม)

- เมื่อพนักงานถูก "ปลดออกจากระบบ" (offboard ตาม operation ที่นิยามไว้ใน [[20260823-005-staff-account-and-authentication|การจัดการบัญชีพนักงานและการยืนยันตัวตน]]) ให้เริ่มนับระยะเวลา **90 วัน** นับจากวันที่ปลดออก
- เมื่อครบ 90 วันนับจากวันที่ปลดออก ระบบต้อง **anonymize ข้อมูลระบุตัวตน** (เช่น ชื่อ, ข้อมูลบัญชีผู้ใช้) ในฟิลด์ "ผู้ทำรายการ" ของทุก audit log record ที่มีพนักงานคนนั้นเป็นผู้ทำรายการ
- Log record เองยังคง**เก็บไว้ต่อไป** (ไม่ลบ) เพื่อประโยชน์ในการตรวจสอบเชิงสถิติ/เหตุการณ์ — เฉพาะข้อมูลระบุตัวพนักงานในฟิลด์ผู้ทำรายการเท่านั้นที่ถูก anonymize
- ระดับ spec นี้ระบุเพียงหลักการทางธุรกิจ ยังไม่ระบุรายละเอียดกลไกทางเทคนิคของการ anonymize (เช่น จะแทนที่ด้วยค่าใดในฟิลด์ใด) — รายละเอียดนั้นจะถูกกำหนดใน `database-schema.md`/`api-spec.md` ต่อไป

## เอกสารที่เกี่ยวข้อง

- [[../02-plan/index|02-plan]] — แผนงานที่จะแตกออกจากเอกสารนี้
- [[20260802-001-table-qr-ordering|ระบบสั่งกาแฟจากโต๊ะด้วยการสแกน QR Code]] — แหล่งเหตุการณ์ออเดอร์/เมนูที่ต้องบันทึก log
- [[20260802-002-sales-dashboard|หน้า Dashboard ดูยอดขาย]] — แหล่งเหตุการณ์การเข้าถึง dashboard ที่ต้องบันทึก log
- [[20260802-004-pdpa-consent|ขอความยินยอม (Consent) ตาม PDPA จากพนักงาน]] — requirement ที่อ้างอิงถึงเอกสารนี้ในการขอความยินยอมเก็บ log การทำงานของพนักงาน
- [[20260823-005-staff-account-and-authentication|การจัดการบัญชีพนักงานและการยืนยันตัวตน]] — ที่มาของ operation "ปลดพนักงานออกจากระบบ (offboard)" ซึ่งเป็น trigger ของการ anonymize ข้อมูลระบุตัวตนใน audit log ข้างต้น
- [[../../03-testing/01-test-plan/test-plan|test-plan.md]] — หัวข้อ 5.4 ระบุ NFR การ anonymize ข้อมูลระบุตัวตนหลัง offboarding 90 วัน ที่ถูกเพิ่มเข้ามาในเอกสารนี้

## หมายเหตุ

- ความสำคัญ/ลำดับก่อนหลัง: กำหนดเป็น Medium priority เนื่องจากเป็น supporting feature เพื่อความโปร่งใส/ตรวจสอบได้ ไม่ใช่ core flow ที่บล็อกการใช้งานร้าน
- ผลการตรวจสอบเอกสารเดิม: grep คำว่า "log", "PDPA", "consent", "ข้อมูลส่วนบุคคล", "audit" (case-insensitive) ในโฟลเดอร์ `01-requirements/01-spec/` แล้วไม่พบเอกสารเดิมที่เกี่ยวข้องหรือทับซ้อน จึงสร้างเป็นเอกสารใหม่แยกต่างหาก
- **อัปเดตวันที่ 2026-08-23:** เพิ่มหัวข้อ "การ anonymize ข้อมูลระบุตัวตนหลังพนักงานถูกปลดออกจากระบบ (Offboarding)" ในส่วน Business Rules — เป็น NFR ใหม่ที่ยืนยันโดย user ระหว่างการทำ `test-plan.md` (ดูหัวข้อ 5.4) ไม่ใช่ requirement ดั้งเดิมตอนสร้างไฟล์นี้ครั้งแรก (2026-08-02) เหตุผล: เพื่อให้สอดคล้องกับหลัก data minimization ของ PDPA เมื่อพนักงานไม่ได้อยู่กับร้านแล้ว โดยใช้ระยะเวลา 90 วันให้เท่ากับนโยบาย retention เดิมของ audit log เพื่อความง่ายในการดำเนินการ (แต่เป็นกลไก/trigger คนละอันกับ retention เดิม)
